{
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "@types/node": "^14.18.36",
    "gts": "^3.1.1",
    "jest": "^29.4.1",
    "mock-stdin": "^1.0.0",
    "nodemon": "^2.0.20",
    "rimraf": "^4.1.2",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1",
    "typescript": "~4.7.0"
  },
  "scripts": {
    "dev": "npx nodemon",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js",
    "test": "npx jest --detectOpenHandles",
    "lint": "gts lint",
    "clean": "gts clean",
    "fix": "gts fix"
  }
}
